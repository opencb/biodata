@namespace("org.opencb.biodata.models.variant.commons")

protocol VariantCommons {

    enum Aggregation {
        NONE,
        BASIC,
        EVS,
        EXAC
    }

     enum Type {
        CASE_CONTROL,
        CASE_SET,
        CONTROL_SET,
        PAIRED,
        PAIRED_TUMOR,
        AGGREGATE,
        TIME_SERIES,
        FAMILY,
        TRIO,
        COLLECTION
    }
}
